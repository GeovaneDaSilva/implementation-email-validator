# Javascript standa
# git commit -m"chore: add npm"

## Padrao de commit git-commit-msg-linter

# npm i -D typescript @types/node

# Livreria para confgurar o linter com Stantard JS: eslint-config-standard-with-typescript

# Plugin: eslint 

# npm i -D husky para excutar comandos antes de una tarefa

# npm i -D lint-staged ele permite rodar script no staged are, = todos que arquivos modificados para dar commits

# npm i -D jest @type/jest ts-jest  = parte de provas || npm install -g jest-cli 
Verify politicy of jest  =>  Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted

OBS: git log exit q

# git commit --amend --no-edit      => juntar commites em um utimo commit sem necessitar text